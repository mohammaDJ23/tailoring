import styled from "styled-components";

export const content = styled.div`
  .MuiIconButton-root {
    padding: 4px;
  }

  .MuiListItem-root {
    padding: 10px 0;
  }
`;

export const text = styled.p``;
